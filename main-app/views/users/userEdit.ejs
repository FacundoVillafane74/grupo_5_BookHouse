<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/stylesForms.css">
    <link rel="stylesheet" href="/css/stylesUserEdit.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.4.0/css/all.css" crossorigin="anonymous">
    <title>Perfil</title>
</head>

<body>
    <div class="big-body">
        <%- include('../partials/bodyLeft.ejs') %>

            <div class="body-rigth">
                <%- include('../partials/header.ejs') %>
                    <div class="card-user login-box">
                        <form action="/user/<%= userFind.id %>/edit?_method=PUT" method="post" enctype="multipart/form-data">
                            <div class="nombre input-global">
                                <label for="name">Nombre del usuario</label>
                                <input type="text" name="name" id="name" value="<%= userFind.name %>">
                                <% if (locals.errors && errors.name) { %>
                                    <p class="text-danger"><%= errors.name %></p>
                                <% } %>
                            </div>

                            <div class="descripcion input-global">
                                <label for="last_name">Apellido del usuario</label>
                                <input type="text" name="last_name" id="last_name" value="<%= userFind.last_name %>">
                                <% if (locals.errors && errors.last_name) { %>
                                    <p class="text-danger"><%= errors.last_name %></p>
                                <% } %>
                            </div>

                            <div class="imagen input-global">
                                <label for="image">Avatar</label>
                                <input type="file" name="image" id="image" class="image-edit">
                                <input type="hidden" name="old-image" id="" value="<%= userFind.image %>">
                            </div>

                            <div class="precio input-global">
                                <label for="email">Email</label>
                                <input type="text" name="email" id="email" value="<%= userFind.email %>">
                                <% if (locals.errors && errors.email) { %>
                                    <p class="text-danger"><%= errors.email %></p>
                                <% } %>
                            </div>

                            <div class="input-global">
                                <input type="submit" value="Editar">
                            </div>
                        </form>
                    </div>
                    <%- include('../partials/footer.ejs') %>
                </div>
    </div>
</body>

</html>