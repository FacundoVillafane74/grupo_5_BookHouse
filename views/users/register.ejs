<!DOCTYPE html>
<html lang="eng">
<head>
    <meta charset="UTF-8";>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/stylesRegister.css">
    <title>Register</title>
</head>
<body>
    <div class="register-box">
    <header>
        <img class="logo" src="/images/products/logo-BookHouse.svg" alt="logo">
    </header>
    <main>
        <form action="/user/register" method="post" enctype="multipart/form-data">

        <div class='nombre-y-apellido input-register'>
            <label for="name">Nombre
                <input type="text" name="name" id="name" placeholder="Pedro" value="<%= locals.oldData && oldData.name ? oldData.name : '' %>">
                <div class="text-danger"></div>
            </label>
            <% if (locals.errors && errors.name) { %>
                <div class="text-danger"><%= errors.name %></div>
            <% } %>
        </div>

        <div class='nombre-y-apellido input-register'>
            <label for="last_name">Apellido
                <input type="text" name="last_name" id="last_name" placeholder="Rodriguez" value="<%= locals.oldData && oldData.last_name ? oldData.last_name : '' %>">
                <div class="text-danger"></div>
            </label>
            <% if (locals.errors && errors.last_name) { %>
                <div class="text-danger"><%= errors.last_name %></div>
            <% } %>
        </div>

        <div class="email input-register">
            <label for="email">Email
                <input type="text" name="email" id="email" placeholder="example@gmail.com" value="<%= locals.oldData && oldData.email ? oldData.email : '' %>">
                <!-- <i class="fa-solid fa-lock"></i> -->
                <div class="text-danger"></div>
            </label>
            <% if (locals.errors && errors.email) { %>
                <div class="text-danger"><%= errors.email %></div>
            <% } %>
            <% if (locals.emailExist) { %>
                <div class="text-danger"><%= emailExist %></div>
            <% } %>
        </div>

        <div class='contraseña input-register'>
            <label for="password">Contraseña
                <input type="password" name="password" id="password" placeholder="Ingresar contraseña">
                <div class="text-danger"></div>
            </label>
            <% if (locals.errors && errors.password) { %>
                <div class="text-danger"><%= errors.password %></div>
            <% } %>
        </div>

        <div class='image-avatar'>
            <label for="image">Imagen
                <input type="file" name="image" id="image">
                <div class="text-danger"></div>
            </label>
            <% if (locals.errors && errors.image) { %>
                <div class="text-danger"><%= errors.image %></div>
            <% } %>
        </div>

        <div id="notificaciones">
            <label for="notifications">
                <input type="checkbox" name="notifications" id="notifications">
                Deseo recibir notificaciones acerca de ofertas y recomendaciones
            </label>
        </div>

        <div id="terminos">
            <label for="termines">
                <input type="checkbox" name="termines" id="termines">
                Acepto los terminos y condiciones
            </label>
        </div>

        <input type="submit" value="Registrar">

        <a href="/user/login" class="tienes-cuenta">Ya tienes cuenta? Iniciar Sesión</a>
    </form>

    </main>
    </div>
    <script src="/js/registerValidation.js"></script>
</body>
</html>