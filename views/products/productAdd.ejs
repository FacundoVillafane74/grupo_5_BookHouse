<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/stylesForms.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
    <title>Login</title>
</head>

<body>
    <div class="big-body">
        <%- include('../partials/bodyLeft.ejs') %>

            <div class="body-rigth">
                <%- include('../partials/header.ejs') %>
                    <div class='login-box'>
                        <main>
                            <form action="/product/add" method="post" enctype="multipart/form-data">

                                <div class="nombre input-global">
                                    <label for="name">Nombre del producto</label>
                                    <input type="text" name="name" id="name" value="<%= locals.old && old.name ? old.name : '' %>">
                                    <% if(locals.errors && errors.name){ %>
                                        <p><%= errors.name.msg %></p>
                                    <% } %>
                                </div>

                                <div class="descripcion input-global">
                                    <label for="description">Descripción</label>
                                    <input type="text" name="description" id="description" value="<%= locals.old && old.description ? old.description : '' %>">
                                    <% if(locals.errors && errors.description){ %>
                                        <p><%= errors.description.msg %></p>
                                    <% } %>
                                </div>

                                <div class="imagen input-global">
                                    <label for="image">Imágen</label>
                                    <input type="file" name="image" id="image">
                                    <% if(locals.errors && errors.image){ %>
                                        <p><%= errors.image.msg %></p>
                                    <% } %>
                                </div>

                                <div class="categoria input-global">
                                    <label for="category">Categoria</label>
                                    <select name="category" id="category">
                                        <option value="">Elegí una categoría</option>
                                        <option value="suspenso" <%= locals.old && old.category === 'suspenso' ? 'selected' : '' %>>Suspenso</option>
                                        <option value="ciencia-ficcion" <%= locals.old && old.category === 'ciencia-ficcion' ? 'selected' : '' %>>Ciencia Ficción</option>
                                    </select>
                                    <% if(locals.errors && errors.category){ %>
                                        <p><%= errors.category.msg %></p>
                                    <% } %>
                                </div>

                                <div class="precio input-global">
                                    <label for="author">Autor</label>
                                    <input type="text" name="author" id="author" value="<%= locals.old && old.author ? old.author : '' %>">
                                    <% if(locals.errors && errors.author){ %>
                                        <p><%= errors.author.msg %></p>
                                    <% } %>
                                </div>

                                <div class="precio input-global">
                                    <label for="age">Año de lanzamiento</label>
                                    <input type="number" name="age" id="age" value="<%= locals.old && old.age ? old.age : '' %>">
                                    <% if(locals.errors && errors.age){ %>
                                        <p><%= errors.age.msg %></p>
                                    <% } %>
                                </div>

                                <div class="precio input-global">
                                    <label for="price">Precio</label>
                                    <input type="number" name="price" id="price" value="<%= locals.old && old.price ? old.price : '' %>">
                                    <% if(locals.errors && errors.price){ %>
                                        <p><%= errors.price.msg %></p>
                                    <% } %>
                                </div>

                                <div class="input-global">
                                    <input type="submit" value="Agregar">
                                </div>

                            </form>
                        </main>
                    </div>
            </div>
    </div>
    <%- include('../partials/footer.ejs') %>
</body>

</html>