// Falta importación del modelo
const { Product } = require('../database/models');

module.exports = {
    home: async (req, res) => {
        try {

        } catch (error) {
            res.send(error);
        }
    },

    /* search: async (req, res) => {
        try {
            
        } catch (error) {
            res.send(error);
        }
    } */        // Ir descomentando a medida que se hagan las rutas y controllers
};
